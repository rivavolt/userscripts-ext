name: release
on: { push: { tags: ["v*"] }, workflow_dispatch: {} }
permissions: { contents: write }
jobs:
  release:
    uses: rivavolt/ci/.github/workflows/avolt-release.yml@v1
    secrets: inherit
